import './bootstrap';
// import Alpine from 'alpinejs';

// window.Alpine = Alpine;

// Alpine.start();

import { Modal, Dropdown, Tooltip, Carousel, initTWE,  Ripple,
    Input, } from "tw-elements";
initTWE({
    Modal,
    Tooltip,
    Carousel,
    Dropdown,
    Ripple,
    Input });

import 'flowbite';
import 'preline'



